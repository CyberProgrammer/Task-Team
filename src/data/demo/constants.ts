import users from './users.json';
import teams from './teams.json';
import team_members from './team_members.json';
import tasks from './tasks.json';
import archived_tasks from './archived_tasks.json';

export const demoUsers = users;
export const demoTeams = teams;
export const demoTeamMembers = team_members;
export const demoTasks = tasks;
export const demoArchivedTasks = archived_tasks;