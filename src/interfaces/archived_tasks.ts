export interface ArchivedTasksInterface {
    id: number
    title: string
    assignee: string
    assigned_to: string
    dueDate: string
    archived_on: string
    status: string
    priority: string
}